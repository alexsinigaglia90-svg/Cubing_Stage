# Backlog To-Do

- [ ] Code-splitting implementeren (3D modules lazy-loaden per view).
- [ ] Performance budget instellen (bundle limieten + fail-on-budget in CI/build).
- [ ] Handheld/shopfloor performance check op low-end hardware (FPS + input latency).
